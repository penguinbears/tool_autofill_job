# 产品规格与开发清单

本文件是产品需求、验收范围和完成状态的统一记录。

状态约定：

- `- [ ]`：尚未完成。
- `- [x] ~~内容~~`：已经完成，并在完成时划掉。
- 部分完成的需求应拆分为更小条目，不把整项提前标记为完成。

## 已实现能力

- [x] ~~导入、编辑、校验并持久化候选人标准 JSON 档案。~~
- [x] ~~扫描当前网页并展示字段匹配、缺失资料和需要人工处理的内容。~~
- [x] ~~填写原生表单控件及多种常见前端框架的自绘组件。~~
- [x] ~~默认保留网页已有值，并提供显式覆盖选项。~~
- [x] ~~自动补足教育和工作经历区块，并按学校或公司匹配档案记录。~~
- [x] ~~填写后回读网页值并报告验证结果。~~
- [x] ~~保存无法识别问题的自定义答案以便复用。~~
- [x] ~~记录、编辑、分类、筛选和排序投递历史。~~
- [x] ~~将投递记录与分类统计导出为 Excel 工作簿。~~
- [x] ~~阻止自动提交、密码填写和本地文件静默上传。~~

## 已完成变更

- [x] ~~移除“未匹配网页诊断”的按钮、页面快照导出逻辑和对应测试。~~

## 待实现：岗位匹配度分析

目标：用户在岗位列表页点击“匹配度”后，扩展批量获取岗位 JD，使用用户配置的大模型服务分析其与候选人经历的匹配程度，并把结果展示在原岗位列表中。

### 列表页交互

- [ ] 在支持的岗位列表页识别岗位卡片、标题和详情链接。
- [ ] 在当前列表页注入“匹配度”入口，并避免重复插入。
- [ ] 支持动态加载、翻页或无限滚动后出现的新岗位。
- [ ] 在每个岗位后显示“非常匹配／匹配／部分匹配／不匹配”状态。
- [ ] 鼠标悬停显示摘要，点击后可固定展开详细分析。
- [ ] 展示等待、抓取、分析、完成和失败状态，并允许重试。

### JD 获取与后台任务

- [ ] 新增 Manifest V3 后台 service worker 和可恢复的任务队列。
- [ ] 对公开页面优先使用后台请求获取 JD。
- [ ] 对需要登录或动态渲染的页面，使用非活动标签页加载并提取 JD。
- [ ] 限制抓取与模型请求并发数，支持取消、超时和错误恢复。
- [ ] 为重点招聘网站提供适配器，并保留通用 JD 提取规则。

### 模型与 Prompt

- [ ] 支持用户配置模型服务商、API 地址、模型名称和个人 API Key。
- [ ] 提供内置匹配分析 Prompt，并允许用户查看、修改和恢复默认值。
- [ ] 只向模型发送匹配所需的脱敏候选人信息和 JD。
- [ ] 要求模型返回经过校验的结构化结果，包括分数、优势、缺口、硬性条件和证据。
- [ ] 由扩展根据固定阈值和硬性条件生成四档匹配等级。
- [ ] 将 JD 视为不可信数据，防止网页内容被当作模型指令。

### 隐私、权限与缓存

- [ ] 在首次使用时解释跨站读取权限和向模型服务商发送数据的范围。
- [ ] API Key 只允许后台可信上下文访问，不传递给岗位页面。
- [ ] 默认使用会话级密钥存储；如支持长期保存，明确说明本机存储风险。
- [ ] 按岗位 URL、JD、候选人档案、Prompt 和模型版本缓存结果。
- [ ] 候选人档案或 JD 变化后自动使旧缓存失效。

### 首版验收范围

- [ ] 至少支持一个明确指定的岗位列表网站。
- [ ] 单次可分析 10 个岗位，且并发受控，不一次性打开大量标签页。
- [ ] 一个用户配置的 OpenAI-compatible 模型接口能够完成端到端分析。
- [ ] 刷新或重新打开列表页后，可以从缓存恢复已完成的匹配结果。
- [ ] 登录失效、验证码、权限拒绝、页面提取失败和模型限流都有可理解的错误提示。
