# 产品规格与开发清单

本文件是产品需求、验收范围和完成状态的统一记录。

状态约定：

- `- [ ]`：尚未完成。
- `- [x] ~~内容~~`：已经完成，并在完成时划掉。
- 部分完成的需求应拆分为更小条目，不把整项提前标记为完成。

## 已实现能力

- [x] ~~导入、编辑、校验并持久化候选人标准 JSON 档案。~~
- [x] ~~扫描当前网页并展示字段匹配、缺失资料和需要人工处理的内容。~~
- [x] ~~填写原生表单控件及多种常见前端框架的自绘组件。~~
- [x] ~~默认保留网页已有值，并提供显式覆盖选项。~~
- [x] ~~自动补足教育和工作经历区块，并按学校或公司匹配档案记录。~~
- [x] ~~填写后回读网页值并报告验证结果。~~
- [x] ~~保存无法识别问题的自定义答案以便复用。~~
- [x] ~~记录、编辑、分类、筛选和排序投递历史。~~
- [x] ~~将投递记录与分类统计导出为 Excel 工作簿。~~
- [x] ~~阻止自动提交、密码填写和本地文件静默上传。~~

## 已完成变更

- [x] ~~移除“未匹配网页诊断”的按钮、页面快照导出逻辑和对应测试。~~
- [x] ~~修复 AI 设置保存反馈不明显的问题：状态前置、保存中反馈并自动定位缺失字段。~~
- [x] ~~识别浏览器尚未加载最新 Manifest 权限的情况，并提示用户在扩展管理页重新加载。~~
- [x] ~~修复 Moka 岗位列表误识别顶部链接的问题：按岗位卡片和 `#/job/...` 路由识别，并把匹配度插入岗位标题旁。~~

## 待实现：岗位匹配度分析

目标：用户在岗位列表页点击“匹配度”后，扩展批量获取岗位 JD，使用用户配置的大模型服务分析其与候选人经历的匹配程度，并把结果展示在原岗位列表中。

### 列表页交互

- [x] ~~在插件弹窗状态区上方增加“AI分析当前页岗位匹配度”按钮。~~
- [x] ~~通过通用规则识别岗位列表页中的岗位卡片、标题和详情链接。~~
- [x] ~~在当前列表页注入匹配度徽标，并避免重复插入。~~
- [ ] 支持动态加载、翻页或无限滚动后出现的新岗位。
- [x] ~~在每个岗位后显示“非常匹配／匹配／部分匹配／不匹配”状态。~~
- [x] ~~鼠标悬停显示摘要，点击后可固定展开详细分析。~~
- [x] ~~展示等待、分析、完成和失败状态；再次点击入口可重试。~~

### JD 获取与后台任务

- [x] ~~新增 Manifest V3 后台 service worker，逐条执行 JD 提取和模型请求。~~
- [ ] 把进行中的批量任务持久化为可恢复队列。
- [ ] 对公开页面优先使用后台请求获取 JD。
- [x] ~~对需要登录或动态渲染的页面，使用非活动标签页加载并提取 JD。~~
- [x] ~~单次最多处理 10 个岗位并串行请求，详情页加载支持超时和错误回退。~~
- [ ] 支持用户主动取消批量任务。
- [x] ~~为 Moka 岗位列表提供专用适配器，并保留通用岗位与 JD 提取规则。~~
- [ ] 为其他重点招聘网站继续增加专用适配器。

### 模型与 Prompt

- [x] ~~在“档案”左侧增加同尺寸“AI”入口，并跳转到独立设置页面。~~
- [x] ~~在 AI 输入区明确提示当前仅支持 OpenAI-compatible Chat Completions，不支持 Anthropic 原生 Messages API。~~
- [x] ~~支持用户配置 Base URL、模型名称和个人 API Key。~~
- [x] ~~提供内置匹配分析 Prompt，并允许用户查看、修改和恢复默认值。~~
- [x] ~~支持读取本地 Markdown/TXT/JSON 文档或公开 GitHub SKILL.md 作为额外分析规则。~~
- [x] ~~只向模型发送匹配所需的脱敏候选人信息和 JD。~~
- [x] ~~要求模型返回经过校验的结构化结果，包括分数、优势、缺口、硬性条件和证据。~~
- [x] ~~由扩展根据固定阈值和硬性条件生成四档匹配等级。~~
- [x] ~~将 JD 视为不可信数据，不允许网页内容覆盖模型安全指令。~~
- [ ] 增加 Anthropic 原生 Messages API Provider 适配器。

### 隐私、权限与缓存

- [x] ~~在 AI 页面解释向模型服务商发送的数据范围，首次匹配时请求对应站点权限。~~
- [x] ~~API Key 只允许扩展可信上下文访问，不传递给岗位页面。~~
- [x] ~~使用会话级密钥存储，浏览器重启后自动清除。~~
- [x] ~~按岗位 URL、JD、候选人档案、Prompt 和模型版本缓存结果。~~
- [x] ~~候选人档案或 JD 变化后自动使旧缓存失效。~~

### 首版验收范围

- [x] ~~支持 Moka 岗位列表页作为首个明确适配的网站。~~
- [x] ~~单次可分析 10 个岗位，串行处理且不一次性打开大量标签页。~~
- [x] ~~一个用户配置的 OpenAI-compatible Chat Completions 接口能够完成端到端分析。~~
- [ ] 刷新或重新打开列表页后，可以从缓存恢复已完成的匹配结果。
- [ ] 登录失效、验证码、权限拒绝、页面提取失败和模型限流都有可理解的错误提示。
