# 智能填表核心升级计划

> 适用基线：v0.13.0  
> 目标：在保持本地档案、默认不覆盖、不填写敏感字段、不自动提交的前提下，提高字段识别、动态页面定位、下拉框和日期填写的成功率。  
> 实施原则：借鉴公开可观察到的工作方式和交互思路，不复制第三方扩展的代码、接口凭证或受保护资源。

## 1. 结论与优先级

当前差距不只是 XPath 和语义匹配，而是完整执行链的差距：

```text
字段语义：决定“填什么”
稳定定位：决定“现在去哪里填”
组件适配：决定“用什么动作填”
回读验证：判断“网页是否真正接受”
重新扫描：处理“填写后 DOM 已经变化”
```

建议按以下优先级实施：

1. **P0：回读验证、重试、重新扫描。** 先消除“代码说填好了，网页实际没接受”的假成功。
2. **P0：稳定定位层。** 不再长期持有一次扫描得到的 DOM 引用；每次填写前重新解析目标。
3. **P0：日期与主流组件适配器。** 日期无法确认的核心在控件状态机和事件，不在语义模型。
4. **P1：候选字段排序与 4B 语义兜底。** 只处理规则难以判断的字段。
5. **P1：下拉选项语义映射。** 解决“硕士”与“硕士研究生”等值域不一致。
6. **P2：远程或本地维护的平台配置、指标与诊断工具。** 用数据持续补齐长尾网站。

## 2. 成功标准

以固定测试网页和真实页面回归集为准，禁止只用“函数返回 filled”作为成功标准。

| 指标 | 第一阶段目标 | 最终目标 |
|---|---:|---:|
| 常见字段语义 Top-1 准确率 | ≥ 95% | ≥ 98% |
| 敏感字段误填次数 | 0 | 0 |
| 普通文本回读成功率 | ≥ 98% | ≥ 99% |
| 已覆盖日期组件填写并确认成功率 | ≥ 90% | ≥ 97% |
| 已覆盖下拉组件填写并确认成功率 | ≥ 90% | ≥ 97% |
| 动态重渲染后定位成功率 | ≥ 90% | ≥ 97% |
| 第二次执行的幂等性 | 不重复改写已成功字段 | 同左 |
| 单页最长自动处理时间 | ≤ 20 秒 | ≤ 15 秒 |

必须继续保持以下安全边界：

- 不点击最终提交按钮。
- 不自动处理验证码。
- 不自动选择本地文件。
- 不把身份证、护照、银行卡等敏感字段交给模型或自动填写。
- 默认不覆盖网页已有值。

## 3. 目标架构

```text
页面扫描
  ↓
字段描述 FieldDescriptor
  ↓
规则层生成 1～8 个候选档案路径
  ├─ 高置信且无冲突 → 直接采用
  └─ 模糊/冲突 → 4B 语义提供器（可选）
  ↓
生成 LocatorBundle（稳定属性 + 相对定位 + XPath 兜底）
  ↓
填写前重新解析元素
  ↓
组件适配器 fill → commit → read
  ↓
按字段类型验证
  ├─ 成功 → 标记本轮成功
  └─ 失败 → 换策略重试
  ↓
等待 DOM 稳定 → 重新扫描 → 增量补填
```

每个字段建议统一成以下内部记录：

```js
{
  id: "本轮稳定字段编号",
  descriptor: { /* 标签、分区、控件类型、附近文本，不含用户填写值 */ },
  locator: { /* 多种定位策略，不只一个绝对 XPath */ },
  semantic: {
    candidates: [],
    selectedPath: "",
    confidence: 0,
    source: "rule | model | custom | manual"
  },
  repeat: { section: "education", group: 0, profileIndex: 0 },
  control: { adapter: "native-text", valueType: "date-month" },
  state: { attempts: 0, verified: false, lastError: "" }
}
```

## 4. 已预留的 4B 模型接口

接口骨架位于 `shared/semantic-provider.js`，当前已被注入页面，但默认关闭，也没有 HTTP 请求或模型实现，因此不会改变现有填表结果。

### 4.1 接口职责

模型只负责：

- 在规则层给出的合法候选路径中选择最合适的一项。
- 返回 `matched`、`ambiguous` 或 `no_match`。
- 给出 0～1 的置信度和短原因。

模型不得负责：

- 生成或执行 XPath。
- 直接操作 DOM。
- 看到完整简历或字段当前值。
- 编造候选集外的档案路径。
- 决定是否覆盖已有值、是否提交表单。

### 4.2 请求示例

```json
{
  "contractVersion": "1.0",
  "task": "match_fields",
  "requestId": "scan-20260902-001",
  "context": {
    "platform": "beisen",
    "locale": "zh-CN"
  },
  "fields": [
    {
      "id": "field-23",
      "descriptor": {
        "label": "开始时间",
        "placeholder": "请选择",
        "sectionText": "教育经历",
        "tag": "div",
        "role": "combobox",
        "required": true
      },
      "candidates": [
        {
          "path": "education[].start_date",
          "ruleScore": 82,
          "reason": "label-and-section"
        },
        {
          "path": "work_experience[].start_date",
          "ruleScore": 62,
          "reason": "label-only"
        }
      ]
    }
  ]
}
```

### 4.3 响应示例

```json
{
  "contractVersion": "1.0",
  "task": "match_fields",
  "requestId": "scan-20260902-001",
  "status": "ok",
  "matches": [
    {
      "id": "field-23",
      "status": "matched",
      "path": "education[].start_date",
      "confidence": 0.94,
      "reason": "section-and-label"
    }
  ]
}
```

接口已经执行以下约束：

- 只允许白名单字段上下文通过边界，`currentValue` 和页面 URL 会被删除。
- 每个字段最多 8 个候选路径，每批最多 80 个字段。
- 返回路径不在候选集中时，整次模型结果会被拒绝。
- 默认关闭；超时、服务不可用、返回格式错误时返回结构化错误，由规则层继续工作。
- 默认超时 2500ms，建议最低采用置信度 0.78。

### 4.4 后续推荐实现方式

优先使用独立的本地模型服务，不把 4B 权重打包进浏览器扩展：

```text
content.js
  → chrome.runtime.sendMessage
background service worker
  → http://127.0.0.1:<固定或可配置端口>/v1/match-fields
本地 4B 服务
```

后续实现任务：

1. 新增 `background.js`，由后台统一与本地服务通信。
2. 在 `manifest.json` 增加后台 worker；仅在用户启用本地模型时申请或说明 localhost 权限。
3. 实现一个 provider，并通过 `JobAutofillSemantic.registerProvider(name, provider)` 注册。
4. 模型服务只监听 `127.0.0.1`，设置请求体大小限制、超时和并发限制。
5. 使用批量请求，避免一个字段调用一次模型。
6. 对字段描述和候选集做哈希缓存；缓存不包含简历值。
7. 设置健康检查，但服务不可用时不能阻塞扫描和规则填写。

不推荐第一版使用浏览器内 WebGPU 直接加载 4B 模型：4-bit 权重本身通常就需要约 2GB，连同运行时和 KV 缓存，常见内存占用会更高；扩展启动速度、显存兼容和包体都会明显变差。

## 5. 各方向详细改造计划

### 5.1 规则匹配层：从“只返回第一名”改为“候选排序”

涉及模块：`CORE-MATCH`、`DATA-MATCH-RULES`  
主要文件：`shared/matcher.js`

实施：

1. 保留现有 `matchField()`，先新增 `rankCandidates(descriptor, options)`，避免一次迁移破坏调用方。
2. 为每个候选返回 `path`、`score`、`reasonCodes`、`matchedAlias`、`sectionMatch`、`typeCompatibility`。
3. 加入字段类型约束：日期、布尔、长文本、电话、邮箱、下拉框等类型不兼容时降权或淘汰。
4. 加入分区层级：页面大区、重复经历区、当前表单项三层上下文分别计分。
5. 增加冲突判断：第一、第二名差距小于 15 分，或多个重复经历路径同分时标记 `ambiguous`。
6. 高置信规则直接采用；只有模糊字段进入模型批次。
7. 敏感字段检查必须发生在生成模型请求之前。

建议阈值初始值：

- 规则分数 ≥ 90 且领先第二名 ≥ 15：直接采用。
- 规则分数 60～89，或领先不足 15：交给语义模型。
- 模型置信度 ≥ 0.78 且路径合法：采用模型结果。
- 其他情况：不自动填写，进入人工处理。

验收：

- 现有别名测试全部通过。
- 新增至少 100 个中英文、不同分区、同名日期字段测试。
- “开始时间”在教育、工作、项目三个分区均不得串区。
- 无分区的“时间”不得强行自动匹配。

### 5.2 稳定定位层：XPath 是兜底，不是唯一答案

涉及模块：`CORE-SCAN`  
建议新增文件：`shared/locator.js`

实施：

1. 扫描时为字段生成 `LocatorBundle`，不要只保存 `record.element`。
2. 定位策略按稳定性排序：
   - 本轮注入的唯一 `data-job-autofill-id`。
   - 稳定且唯一的 `id`、`name`、`data-*`、ARIA 关系。
   - `label[for]`、表单项容器、分区标题和重复组索引的组合定位。
   - 相对 CSS 定位。
   - 结构化 XPath 兜底。
3. XPath 生成时跳过随机哈希 class，优先稳定属性，并保存标签/分区/控件类型指纹。
4. 每次填写前调用 `resolveLocator(locator)` 获取当前元素，不直接信任旧 DOM 引用。
5. 解析到元素后检查指纹；标签、分区或类型明显不一致时拒绝填写并重新扫描。
6. iframe 分开扫描，定位记录必须包含 frame 标识；第一版可以明确只支持同源 frame。
7. Shadow DOM 后续使用分段 locator，不用一个普通 XPath 穿透。

验收：

- 测试页在扫描后替换整个表单节点，仍能找到正确字段。
- 同一区域有多个“开始时间”时，依靠分区和重复组不串填。
- 随机 class 每次变化时仍可定位。
- XPath 失效时能重新扫描，不得误填附近的另一个输入框。

### 5.3 组件适配器：统一 `fill / commit / read / verify`

涉及模块：`CORE-INPUT`、`CORE-SELECT`、`CORE-DATE`、`CORE-PLATFORM`  
建议新增目录：`adapters/`

建议接口：

```js
{
  name: "beisen-date",
  priority: 100,
  supports(record, element, platform) {},
  async fill(context) {},
  async commit(context) {},
  read(context) {},
  verify(context) {},
  async cleanup(context) {}
}
```

实施顺序：

1. `native-text`：input、textarea、contenteditable。
2. `native-select`、radio、checkbox。
3. `native-date`：date、month、datetime-local、time。
4. `moka-select`、`moka-month-range`。
5. `beisen-phoenix-select`、`beisen-date`。
6. `ant-select`、`ant-picker`、range picker。
7. `element-select`、`element-date`。
8. 其他 Arco、TDesign、Naive、Semi 组件按真实失败样本追加。

每个适配器都必须自己声明“如何读取真实值”和“成功判定”，不能只在点击后返回 `filled`。

### 5.4 日期填写：实现状态机和精确确认

涉及模块：`CORE-DATE`

实施：

1. 新增纯函数日期解析层：接受 `YYYY-MM`、`YYYY-MM-DD`，输出 `{year, month, day, precision}`。
2. 根据目标控件需要格式化，不把所有日期一律补成 1 日。
3. 原生日期使用原型 setter，并按顺序触发 `focus → input → change → blur`。
4. 自绘日期使用状态机：
   - 打开面板。
   - 确认弹层与当前输入框关联。
   - 识别当前年份/月。
   - 有界地切换年、月面板。
   - 点击精确日期或月份。
   - 点击确定，或按组件要求 Enter/Tab/失焦。
   - 等待弹层关闭或输入框显示值变化。
   - 回读并比较标准化值。
5. 年份最多导航 60 次、月份最多 12 次；超过即失败，禁止无限循环。
6. 处理范围日期时，把开始和结束作为一个组件事务，避免只成功一半。
7. 面板中的禁用日期、超范围年份和“至今”要返回明确状态。

日期成功条件必须同时满足：

- 输入框或组件状态可读到目标年月/日期。
- 必要的确定动作已经发生。
- 组件没有恢复为空值。

### 5.5 网页事件与框架状态同步

涉及模块：`CORE-INPUT`

实施：

1. 抽出 `setNativeProperty()`，分别支持 input、textarea、select、checked。
2. 提供三档写入策略：
   - `native-setter`：原型 setter + 标准事件。
   - `simulated-typing`：keydown/keypress/input/keyup，按字符输入。
   - `component-action`：点击选项、Enter、Tab、失焦。
3. 事件必须设置 `bubbles: true`，跨 Shadow DOM 场景按需设置 `composed: true`。
4. React/Vue 受控组件回滚时，自动升级到下一档策略。
5. 每个策略后等待短暂稳定并回读，不使用固定长等待替代状态检测。

### 5.6 回读验证、重试和状态定义

涉及模块：`CORE-INPUT`、`CORE-SELECT`、`CORE-DATE`

统一结果状态：

```text
verified-filled       已填写且回读一致
existing-value        按策略保留原值
missing-value         档案没有值
unmatched             未找到语义路径
ambiguous             语义结果不确定
stale-locator         原定位已失效
option-not-found      没有可接受选项
commit-failed         已写入但组件未确认
verification-failed   回读值不一致或又被清空
unsupported-control   没有对应适配器
sensitive-skipped     敏感字段跳过
```

实施：

1. 为文本、日期、枚举、多选分别实现 `normalizeExpected()` 和 `normalizeActual()`。
2. 填写动作结束后等待以下任一事件：目标值出现、弹层关闭、DOM mutation 停止、超时。
3. 单字段最多尝试 2 次；第二次必须更换策略，不能机械重复同一点击。
4. 失败后保存实际读值、适配器、尝试步骤和原因，供诊断导出。
5. UI 统计中“填充成功”只计算 `verified-filled`。

### 5.7 多轮重新扫描与增量补填

涉及模块：`CORE-SCAN`、`CORE-REPEAT`

实施：

1. 把当前单轮填写改成有界调度器：最多 3 个普通轮次，重复区块扩展可另有 1 个准备轮次。
2. 每轮顺序：`scan → match → fill → waitForStableDom → rescan`。
3. 用“字段语义身份”记录成功项：`path + profileIndex + section fingerprint`，不要仅用 XPath。
4. 下一轮只处理新出现、上轮定位失效或可重试失败的字段。
5. 如果一轮没有新增字段、没有成功项、DOM 也没变化，立即结束。
6. 每轮都重新计算重复经历对应关系，避免新增教育经历后数组索引串位。
7. 页面导航、URL 改变或用户关闭弹窗时取消当前任务。

验收：

- 选择学历后动态出现“专业类别”字段，第二轮能识别并填写。
- 新增经历后原节点全部重建，仍能继续填写。
- 已成功字段不在后续轮次重复触发事件。
- 三轮后仍失败必须结束并显示原因。

### 5.8 下拉选项语义映射

涉及模块：`CORE-SELECT`  
建议后续为语义接口增加独立任务：`match_options`

先做确定性归一化：

- 学历：硕士、硕士研究生、研究生（硕士）。
- 学位：工学硕士、硕士学位。
- 城市：北京、北京市、北京-北京市。
- 布尔：是/否、有/无、Yes/No。
- 熟练度：熟练、精通、良好等可配置等级。

只有规则无法选择时，再把“目标标准值 + 页面候选选项 + 字段上下文”交给本地模型。返回值必须是页面候选项的 ID 或索引，不能返回自由文本。

验收：

- 模型返回页面候选外的值时拒绝执行。
- 同名选项必须结合所属弹层和当前字段定位。
- 多选不得超过网站限制；已经选中的选项不得重复点击。

### 5.9 平台配置与可维护性

涉及模块：`CORE-PLATFORM`

实施：

1. 把散落在 `content.js` 的平台选择器迁移成版本化配置对象。
2. 配置包含平台识别、表单项、标签、弹层、选项、日期面板、重复区块按钮。
3. 本地内置配置始终可用；未来如果支持更新配置，必须校验签名/版本并允许回滚。
4. 平台适配器只依赖配置和统一接口，减少大型 `if/else`。
5. 不引入第三方扩展的线上 API、登录态、Cookie 或远程配置地址。

### 5.10 诊断与用户界面

涉及模块：`UI-SUMMARY`、`UI-ATTENTION`、`UI-DIAGNOSTIC`

实施：

1. 扫描结果展示匹配来源：规则、模型、自定义答案、人工。
2. 展示置信度，但不让用户误以为高置信就是填写成功。
3. 对日期和下拉失败显示具体阶段：未打开、未找到选项、未确认、回读失败。
4. 诊断文件增加经过脱敏的 descriptor、候选分数、locator 类型、适配器、每次尝试和实际回读结果。
5. 默认不导出表单当前值；用户主动选择时也要单独提示可能含个人信息。
6. 增加“仅重新处理失败项”，方便人工调整页面后继续。

## 6. 分阶段实施与验收

后续模型应一次只完成一个阶段，每阶段通过测试后再进入下一阶段。

### 阶段 0：建立基线与失败样本

- 保存 Moka、北森、Ant、Element 的脱敏 HTML fixture。
- 为现有字段扫描、文本、下拉、日期建立成功率基线。
- 建立 `tests/fixtures/` 和期望结果 JSON。
- 输出每个失败的分类，不允许只记录“没填上”。

完成标准：可以重复运行同一组测试并得到稳定结果。

### 阶段 1：回读验证和统一状态（最高优先级）

- 抽出读值、标准化、验证函数。
- 替换点击后直接返回 `filled` 的分支。
- 更新 popup 成功统计和提示。
- 添加验证失败、受控组件回滚测试。

完成标准：不存在未经回读的“成功”。

### 阶段 2：稳定 Locator 与重渲染恢复

- 新增 `shared/locator.js`。
- 扫描记录改存 LocatorBundle。
- 填写前重新解析并校验指纹。
- 添加节点替换、随机 class、同名字段测试。

完成标准：扫描后 DOM 重建仍能安全定位；找不到时失败而不是猜。

### 阶段 3：多轮调度器

- 新增最多 3 轮的增量填写循环。
- 使用 MutationObserver 等待短暂 DOM 稳定。
- 增加成功身份去重和取消机制。
- 覆盖动态出现字段与重复区块重建。

完成标准：动态字段能补填，无死循环和重复填写。

### 阶段 4：组件适配器重构

- 建立 adapter registry。
- 先迁移原生组件，再迁移 Moka、北森。
- 保持旧实现作为短期回退，完成后删除重复分支。
- 每个适配器建立独立 fixture。

完成标准：现有测试不降级，适配器可独立选择、填写、读取和验证。

### 阶段 5：日期状态机

- 实现日期解析、格式化和精度。
- 完成原生、Moka、北森、Ant、Element 日期适配器。
- 增加跨年、仅年月、范围、至今、禁用日期测试。

完成标准：覆盖组件的日期在回读后与目标一致，且弹层已正确确认/关闭。

### 阶段 6：候选排序与 4B 语义接入

- 在 matcher 增加 `rankCandidates()`。
- 批量挑出 ambiguous 字段。
- 实现 background/localhost provider。
- 严格使用已预留契约校验结果。
- 增加服务关闭、超时、错误 JSON、越权路径、低置信测试。

完成标准：关闭模型时功能与当前规则版本等价；开启后只改善模糊字段，模型故障不阻塞填写。

### 阶段 7：下拉选项语义与配置化平台层

- 先建立枚举同义词表，再增加 `match_options` 契约。
- 把平台选择器迁移到本地版本化配置。
- 根据真实失败样本补齐平台，不追求一次覆盖所有框架。

完成标准：选项选择始终来自当前弹层候选集，平台配置可单独测试。

### 阶段 8：灰度、指标与文档

- 为语义模型、增量多轮、适配器新架构设置独立开关。
- 保存本地聚合指标：扫描数、匹配来源、验证成功/失败类别，不保存字段值。
- 对 20～30 个真实申请页面做人工复核回归。
- 达标后再默认开启；保留一版旧执行链回退开关。

## 7. 文件级改动清单

| 文件/目录 | 计划 |
|---|---|
| `shared/matcher.js` | 增加候选排序、冲突判断、类型兼容；保留兼容入口 |
| `shared/semantic-provider.js` | 已预留字段语义契约；后续不要塞入具体模型代码 |
| `shared/locator.js` | 新增 LocatorBundle 生成、解析、指纹校验 |
| `adapters/` | 新增控件适配器注册表及平台适配器 |
| `content.js` | 缩减为扫描编排、任务调度、消息入口；逐步迁出组件细节 |
| `background.js` | 后续新增本地模型通信、超时和健康状态 |
| `manifest.json` | 后续登记 background；模型未实现前不要增加 localhost 权限 |
| `popup.js/html/css` | 展示匹配来源、验证结果、失败阶段和模型状态 |
| `shared/storage.js` | 保存功能开关、模型配置和不含字段值的本地指标 |
| `tests/semantic-provider.test.js` | 已增加契约、安全边界和越权路径测试 |
| `tests/fixtures/` | 后续新增平台与动态 DOM 回归样本 |

## 8. 对后续模型的执行要求

每个实现任务都应遵守：

1. 开始前读取本文件、`modules.md`、相关源码和现有测试。
2. 一次只实施一个阶段，列出会修改的文件和兼容策略。
3. 不删除用户档案、不修改真实候选人数据、不点击真实网页提交。
4. 新逻辑必须有单元测试或 DOM fixture 测试。
5. 报告真实回读结果，不能以 `click()` 没抛异常作为成功。
6. 新增平台适配必须基于脱敏页面样本，不用宽泛选择器猜测全站。
7. 模型调用失败、关闭或未安装时，规则匹配必须正常工作。
8. 模型只能看到字段结构和候选路径；除非未来单独评审，不得发送简历值。
9. 每阶段结束更新本文件的完成状态、测试命令和已知限制。

推荐后续任务描述模板：

```text
请实施 AUTOFILL-CORE-ROADMAP.md 的“阶段 N”。
只处理该阶段，不提前实现后续阶段。
先核对现有实现与测试，再修改代码。
必须保留默认不覆盖、不填敏感字段、不自动提交的安全边界。
完成后运行相关单元测试和 DOM fixture 测试，并报告：
1. 修改文件；2. 行为变化；3. 测试结果；4. 剩余限制。
```

## 9. 当前已完成的准备工作

- [x] 定义 4B 字段语义匹配契约。
- [x] 默认关闭语义匹配，不包含网络实现。
- [x] 请求白名单脱敏，不传页面 URL 和当前表单值。
- [x] 限制模型只能返回候选路径。
- [x] 为关闭状态、正常结果和越权路径增加契约测试。
- [ ] 阶段 0：基线与失败样本。
- [x] 阶段 1：回读验证（v0.13.0；原生文本、原生下拉、自绘控件、复选框统一回读；浏览器回归待补齐依赖）。
- [ ] 阶段 2：稳定 Locator。
- [ ] 阶段 3：多轮调度器。
- [ ] 阶段 4：适配器重构。
- [ ] 阶段 5：日期状态机。
- [ ] 阶段 6：4B 服务实际接入。
- [ ] 阶段 7：选项语义和平台配置。
- [ ] 阶段 8：灰度与回归。
